<%= form_tag charges_path do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
    <h1>Would you like to upgrade to a premium account?</h1>
    <div class="row">
  <div class="col-md-4">
    <p>Premium members recieve the following benefits for a small fee of  <label class="amount">
      <span> <%= pretty_amount(@amount) %></span>
    </label>:</p>
    <ul>
      <li>You will be able to create private wikis.</li>
      <li>Private wikis will be hidden from the public.</li>
      <li>We take our premium members' information seriously to protect your wikis from standard users.</li>
    </ul>
  </div>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="<%= @description %>"
          data-amount="<%= @amount %>"
          data-email="<%= current_user.email %>"
          data-locale="auto"></script>
<% end %>